[package]
name = "dsh-runtime"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
flate2 = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
semver = "1"
tar = "0.4"

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
strip = true
